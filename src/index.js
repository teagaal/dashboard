import React from 'react'
import ReactDOM from 'react-dom'
import LoginActivity from './containers/LoginActivity'

ReactDOM.render(
  <React.StrictMode>
    <LoginActivity />
  </React.StrictMode>,
  document.getElementById('root')
)
